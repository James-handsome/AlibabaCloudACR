name: "Docker Image Sync to ACR"

on:
  workflow_dispatch:
    inputs:
      image_name:
        description: 'Docker Hub 镜像名 (例如: mysql:8.0)'
        required: true
        default: 'mysql:latest'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Alibaba Cloud ACR
        uses: docker/login-action@v3
        with:
          registry: crpi-tjpbhu7ev24zh1rq.cn-hangzhou.personal.cr.aliyuncs.com
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Pull and Push to Aliyun
        run: |
          # 1. 获取输入值
          RAW_IMAGE="${{ github.event.inputs.image_name }}"
          
          # 2. 从 Docker Hub 拉取
          echo "📥 正在拉取源镜像: $RAW_IMAGE"
          docker pull "$RAW_IMAGE"
          
          # 3. 核心修复：处理标签格式（仅替换非法字符，保留大小写）
          # 仅将 / 和 : 替换为 _，不再使用 tr 转换小写
          SAFE_TAG=$(echo "$RAW_IMAGE" | sed 's/[\/:]/_/g')
          
          # 4. 拼接最终镜像地址
          # 注意：这里的仓库路径 james-hub 必须是小写的（由阿里云后台决定）
          # 我们只在冒号后的 Tag 部分保留可能的大写
          TARGET_IMAGE="crpi-tjpbhu7ev24zh1rq.cn-hangzhou.personal.cr.aliyuncs.com/garendeng_zhanhao/james-hub:$SAFE_TAG"
          
          # 5. 打标签并推送
          echo "📤 正在推送至阿里云: $TARGET_IMAGE"
          docker tag "$RAW_IMAGE" "$TARGET_IMAGE"
          docker push "$TARGET_IMAGE"
          
          echo "=========================================================="
          echo "✅ 同步成功！"
          echo "🚀 阿里云拉取命令："
          echo "docker pull $TARGET_IMAGE" 
          echo "📦 源镜像名称: $RAW_IMAGE"
          echo "🏷️ 转换后标签: $SAFE_TAG"
          echo "=========================================================="
